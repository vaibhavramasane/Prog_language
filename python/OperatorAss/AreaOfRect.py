#Q 4. Write a java program to enter length and breadth of a rectangle and find its area.
#Formula - area= length * breadth;

len = float(input("Ente length: "));
bre = float(input("Ente breadth: "));

area = len * bre;
print(area);
